import uuid

def create_record(workspace_id: str, name: str, email: EmailStr, company: str, city: str) -> dict:
    record_id = f"rec_{uuid.uuid4().hex[:8]}"
    is_valid = 
    tag
    processed_at
    created_at